{
  "name": "whatwg-encoding",
  "description": "Decode strings according to the WHATWG Encoding Standard",
  "keywords": [
    "encoding",
    "whatwg"
  ],
  "version": "2.0.0",
  "author": "Domenic Denicola <d@domenic.me> (https://domenic.me/)",
  "license": "MIT",
  "repository": "jsdom/whatwg-encoding",
  "main": "index.js",
  "files": [
    "index.js",
    "webidl2js-wrapper.js",
    "lib/"
  ],
  "scripts": {
    "build": "node scripts/update.js && node scripts/convert-idl.js",
    "prepare": "node scripts/update.js && node scripts/convert-idl.js",
    "pretest": "node scripts/get-latest-platform-tests.js && node scripts/update.js && node scripts/convert-idl.js",
    "build-live-viewer": "browserify index.js --standalone whatwgEncoding > live-viewer/whatwg-encoding.js",
    "coverage": "jest --coverage",
    "lint": "eslint .",
    "test": "jest"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3",
    "webidl-conversions": "^7.0.0"
  },
  "devDependencies": {
    "@domenic/eslint-config": "^2.0.0",
    "browserify": "^17.0.0",
    "eslint": "^8.23.1",
    "got": "^11.8.5",
    "jest": "^29.0.3",
    "webidl2js": "^17.1.0"
  },
  "engines": {
    "node": ">=12"
  },
  "jest": {
    "collectCoverageFrom": [
      "lib/**/*.js",
      "!lib/utils.js"
    ],
    "coverageDirectory": "coverage",
    "coverageReporters": [
      "lcov",
      "text-summary"
    ],
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/test/**/*.js"
    ],
    "testPathIgnorePatterns": [
      "^<rootDir>/test/testharness.js$",
      "^<rootDir>/test/web-platform-tests/"
    ]
  }
}
